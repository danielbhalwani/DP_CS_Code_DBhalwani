# min = scores 
# min = scores
# sum = 0 
#average = 0
# loop N frmo 0 to scores. length - 1
	# if scores i > max
	#max = scores
	# else if scores < min
	# min = scores

#overloaded methods have the same name but are distinguished with different parameters
